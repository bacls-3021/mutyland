<span class="step">Bước 1 Đăng ký</span>
<p> Mở tab mới <a href="https://nhadat24h.com/dang-ky">https://nhadat24h.com/dang-ky</a></p>
<p> Bấm F12, vào tab console dán dòng code dưới đây rồi gõ Enter </p>
<div class="box">
  <code class="hightlight">
    $('#user_name').val(<%= inra(product.email_fake) %>)
    $('#user_pass').val('Ketcdlena1@@')
    $('#user_pass_confirm').val('Ketcdlena1@@')
    $('input[name="fullname"]').val(<%= inra(product.name) %>)
    $('input[name="mobile"]').val(<%= inra(product.phone) %>)
    $('input[name="agree"]').click()
  </code>
  <button class="btn btn-primary copy" onClick="copy(this)">Copy</button>
</div>

<span class="step">Bước 2 Đăng nhập</span>
<p>Vào <a href="https://nhadat24h.com/dang-nhap">https://nhadat24h.com/dang-nhap</a></p>
<div class="box">
  <code class="hightlight">
    $('#user_name').val(<%= inra(product.email_fake) %>)
    $('#user_pass').val('Ketcdlena1@@')
    $('#fLogin').submit()
  </code>
  <button class="btn btn-primary copy" onClick="copy(this)">Copy</button>
</div>

<span class="step">Bước 3 Đăng bài</span>
<p>Vào <a href="https://nhadat24h.com/dang-tin">https://nhadat24h.com/dang-tin</a></p>
<div class="box">
  <code class="hightlight">
    $('#title').val(<%= inra(product.title) %>)
    CKEDITOR.instances['mota'].setData(<%= inra(product.description) %>)
    $('#cat_id0').val(10)
    $('#display_email').val(0)

    <% @product.product_images.each_with_index do |img, i| %>
      <% break if i == 10 %>
      const url<%= i %> = <%= inra(img.full_url) %>
      const fileName<%= i %> = <%= inra("myFile#{i}.jpg") %>

      fetch(url<%= i %>)
        .then(async response => {
          const contentType<%= i %> = response.headers.get('content-type')
          const blob<%= i %> = await response.blob()
          const file<%= i %> = new File([blob<%= i %>], fileName<%= i %>, { contentType<%= i %> })

          const dataTransfer<%= i %> = new DataTransfer();
          dataTransfer<%= i %>.items.add(file<%= i %>);
          document.getElementById('hinhanh<%= i + 1 %>').files = dataTransfer<%= i %>.files
        })
    <% end %>

  </code>
  <button class="btn btn-primary copy" onClick="copy(this)">Copy</button>
</div>
